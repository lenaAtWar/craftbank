function clearMonik(yStart, yEnd)
	for y=yStart, yEnd do
		term.setCursorPos(1, y)
		term.clearLine()
	end
end

function array_reverse(array)
	reverse = {}
	i = 1
	for key, value in pairs(array) do
		key = #array - i + 1

		reverse[key] = value

		i = i + 1
	end

	return reverse
end